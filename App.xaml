<Application x:Class="KajsaJosefssonCV.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:KajsaJosefssonCV.ViewModels"
             xmlns:views="clr-namespace:KajsaJosefssonCV.Views">
    <Application.Resources>
        <!-- Global font -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <!-- DataTemplates för UserControls (flikar) -->
        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:AboutViewModel}">
            <views:AboutView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ExperienceViewModel}">
            <views:ExperienceView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:EducationViewModel}">
            <views:EducationView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:SkillsViewModel}">
            <views:SkillsView />
        </DataTemplate>
    </Application.Resources>
</Application>