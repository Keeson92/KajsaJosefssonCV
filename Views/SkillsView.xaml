<UserControl x:Class="KajsaJosefssonCV.Views.SkillsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer Margin="20" Padding="0 0 20 0">
        <StackPanel>
            <TextBlock Text="Kompetenser"
                       FontSize="22"
                       FontWeight="Bold"
                       Margin="0 0 0 12"/>

            <ItemsControl ItemsSource="{Binding Skills}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0 0 0 8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- NAMN -->
                            <TextBlock Text="{Binding Name}"
                                       Grid.Column="0"
                                       TextWrapping="Wrap"
                                       FontSize="16"/>

                            <!-- NIVÅ -->
                            <TextBlock Text="{Binding Level}"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       Foreground="Gray"
                                       FontSize="16"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>