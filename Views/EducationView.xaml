<UserControl x:Class="KajsaJosefssonCV.Views.EducationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer Margin="20">
        <StackPanel>
            <TextBlock Text="Utbildning" FontSize="22" FontWeight="Bold" Margin="0 0 0 12"/>
            <ItemsControl ItemsSource="{Binding Educations}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0 0 0 12">
                            <TextBlock Text="{Binding Degree}" FontSize="16" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Institution}" FontStyle="Italic" />
                            <TextBlock Text="{Binding Period}"/>
                            <TextBlock Text="{Binding Notes}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
