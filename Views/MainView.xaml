<Window x:Class="KajsaJosefssonCV.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:KajsaJosefssonCV.ViewModels"
        mc:Ignorable="d"
        Title="Mitt CV" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="FloralWhite">

    <!-- Sätt DataContext -->
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#B399E6" Padding="12" BorderBrush="#8C66CC" BorderThickness="0,0,0,1" Margin="0,0,0,10">
            <DockPanel>
                <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="8">
                    <!-- Namn -->
                    <TextBlock Text="{Binding HeaderName}" FontSize="22" FontWeight="ExtraBlack" Foreground="Beige"/>

                    <!-- Email och telefonnummer -->
                    <TextBlock Text="{Binding HeaderEmail}" FontSize="14" Foreground="Beige" Margin="0,2,0,0"/>
                    <TextBlock Text="{Binding HeaderPhone}" FontSize="14" Foreground="Beige" Margin="0,0,0,5"/>

                    <!-- Titel -->
                    <TextBlock Text="{Binding HeaderTitle}" FontSize="13" FontWeight="Bold" Foreground="Beige" TextWrapping="Wrap" MaxWidth="600" TextAlignment="Left" Width="454"/>

                    <!-- Export-knapp -->
                    <Button Content="Exportera till PDF" Command="{Binding ExportPdfCommand}" HorizontalAlignment="Right" Margin="0,10,0,0" Padding="10,5" FontWeight="Bold" FontSize="14" Foreground="Beige" Width="140" Height="30">
                        <Button.Background>
                            <SolidColorBrush Color="#8C66CC"/>
                        </Button.Background>
                        <Button.BorderBrush>
                            <SolidColorBrush Color="#7B68EE"/>
                        </Button.BorderBrush>
                        <Button.BorderThickness>1</Button.BorderThickness>
                        <Button.Cursor>Hand</Button.Cursor>
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#D3B3FF"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#8C66CC"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- TabControl med förbättrade flikar -->
        <TabControl Grid.Row="1"
                    ItemsSource="{Binding Tabs}"
                    SelectedItem="{Binding SelectedTab}"
                    Background="Beige"
                    BorderBrush="Transparent"
                    BorderThickness="1"
                    Margin="10,0,10,10">

            <!-- Styla flikarna -->
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="14,6"/>
                    <Setter Property="Margin" Value="2,0"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Bd"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="8,8,0,0"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter ContentSource="Header"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#B399E6"/>
                                        <Setter Property="Foreground" Value="Beige"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#D3B3FF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.ItemContainerStyle>

            <!-- Header styrs av ItemTemplate -->
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding TabHeader}" Padding="10,6" FontWeight="SemiBold"/>
                </DataTemplate>
            </TabControl.ItemTemplate>

            <!-- Content visas via DataTemplates i App.xaml -->
        </TabControl>
    </Grid>
</Window>